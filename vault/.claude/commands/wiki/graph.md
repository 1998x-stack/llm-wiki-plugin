---
description: "构建知识图谱 graph.json（wiki:build 的别名）"
---

# wiki:graph

> 此命令是 `wiki:build` 的别名。请执行 `wiki:build` 的完整流程。

执行 wiki:build 的完整流程：lint 检查 → 构建 graph.json → 同步 static/ → 构建 statistics + wiki HTML。
