---
type: concept
status: active
confidence: 0.95
created: 2026-04-20
updated: 2026-04-20
last_accessed: 2026-04-20
source_count: 1
tags: ["机器学习", "优化", "深度学习", "数值计算"]
aliases: ["Gradient Descent", "梯度下降法", "最速下降法", "GD"]
relates_to:
  - target: "[[反向传播]]"
    type: uses
  - target: "[[随机梯度下降（SGD）]]"
    type: extends
  - target: "[[Momentum（动量）]]"
    type: extends
supersedes: null
---

# 梯度下降（Gradient Descent）

## 概述
梯度下降是最基础的优化算法，通过沿损失函数梯度的反方向迭代更新参数，逐步寻找损失的最小值，是所有神经网络训练的核心引擎。

## 关键内容
1. **核心直觉**：把损失函数想象成多维山地地形，梯度指向"最陡的上坡方向"。我们沿梯度的**反方向**移动（下山），每次移动一步，步长由学习率 η 控制。更新规则：W ← W - η · ∂L/∂W。
2. **学习率的关键作用**：学习率 η 决定每次更新的步长。太大 → 可能越过最低点甚至发散；太小 → 收敛极慢。学习率的选择和调度（learning rate schedule）是训练神经网络最重要的超参数之一。
3. **与反向传播的关系**：[[反向传播]]负责计算梯度（∂L/∂W），梯度下降负责使用梯度更新参数。两者配合完成一次训练迭代：前向传播 → 计算损失 → 反向传播求梯度 → 梯度下降更新权重。
4. **变体家族**：基础梯度下降使用全量数据计算梯度（Batch GD），计算成本高。[[随机梯度下降（SGD）]]每次用一个样本，[[Momentum（动量）]]引入惯性加速收敛，[[Adam（自适应矩估计）]]结合动量和自适应学习率，[[RMSProp]]自适应调整每参数学习率。这些变体构成了现代深度学习优化的工具链。
5. **非凸优化的挑战**：神经网络的损失面是非凸的，存在多个局部最优、[[鞍点（Saddle Point）]]和平坦区域。梯度下降不能保证找到全局最优，但在实践中通常能找到足够好的解。

## 来源
- [[paper_02_backpropagation]] — 直觉理解：爬山与下山章节

## 相关
- [[反向传播]] — uses
- [[随机梯度下降（SGD）]] — extends
- [[Momentum（动量）]] — extends
- [[Adam（自适应矩估计）]] — extends
- [[AdamW]] — extends
- [[RMSProp]] — extends
- [[均方误差（MSE）]] — optimizes
- [[鞍点（Saddle Point）]] — challenges
