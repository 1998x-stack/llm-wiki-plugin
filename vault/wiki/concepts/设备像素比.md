---
type: concept
status: active
confidence: 0.9
created: 2026-04-16
updated: 2026-04-16
last_accessed: 2026-04-16
source_count: 1
tags: [DPR, 分辨率, 高DPI, Retina, 渲染, 跨平台]
aliases: [DPR, device pixel ratio, 设备像素比DPR, 像素密度比]
relates_to: [NanoVG分辨率模式, 等距柱状投影]
supersedes: null
---
# 设备像素比

## 概述
设备像素比（D[[Probabilistic Robotics|PR]]，Device [[Pi-Agent|Pi]]xel Ratio）是物理像素数与逻辑像素数之比，决定高 DPI 屏幕（Retina/高清屏）上的渲染清晰度。常见值：1.0（普通屏）、2.0（Retina）、3.0（超高密度屏）。

## 关键内容
1. **核心关系**：`系统逻辑分辨率 = 物理分辨率 / DPR`。在 [[UrhoX引擎|UrhoX]] 中通过 `graphics:GetDPR()` 获取，物理分辨率通过 `graphics:GetWidth()/GetHeight()` 获取。
2. **高DPI适配意义**：若忽略 D[[Probabilistic Robotics|PR]] 直接以物理像素为单位绘制（模式C），在 D[[Probabilistic Robotics|PR]]=2 的屏幕上 UI 尺寸仅为设计稿的 1/4，导致界面过小；正确传入 `pixelRatio=dpr` 给渲染器可确保清晰度与尺寸正确。
3. **输入坐标转换**：引擎鼠标/触摸输入 API 返回物理像素坐标，转换到逻辑坐标需除以 D[[Probabilistic Robotics|PR]]：`logicalX = inputX / dpr`；进一步转到设计坐标还需除以设计缩放比 scale。

## 来源
- [[raw/articles/personal/ai-dev-kit/.claude/skills/nvg-resolution-mode/SKILL.md]] — NanoVG 分辨率模式编写范式，UrhoX Lua AI 开发指南

## 相关
- [[NanoVG分辨率模式]] — relates_to，DPR 是 NanoVG 三种分辨率模式的核心变量
