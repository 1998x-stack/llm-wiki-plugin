---
type: concept
status: active
confidence: 0.85
created: 2026-04-26
updated: 2026-04-26
last_accessed: 2026-04-26
source_count: 1
tags: [推荐系统, 图神经网络, 深度学习]
aliases: [层组合, Layer Combination]
relates_to: 
  - {target: "图神经网络", type: uses}
  - {target: "LightGCN", type: implements}
  - {target: "嵌入平滑", type: addresses}
supersedes: null
---

# 层组合

## 概述
层组合是图神经网络中的一种策略，通过将模型不同层的输出进行加权组合来生成最终的节点表示。这种方法旨在保留多尺度信息并缓解深层网络中的过度平滑问题。

## 关键内容

1. **基本定义**：
   - 将网络中第0层到第K层的节点嵌入进行加权求和
   - 公式：e_u = Σ_k α_k * e_u^(k)，其中α_k为第k层的组合系数
   - 在[[LightGCN]]中常采用均匀权重α_k = 1/(K+1)

2. **设计动机**：
   - 解决深层图网络中的信息丢失问题
   - 缓解过度平滑现象，保持节点嵌入的[[区分]]度
   - 捕获多尺度的邻域信息（第1层反映直接邻居，第2层反映二跳邻居等）

3. **核心优势**：
   - 保留初始信息：第0层嵌入（ID嵌入）始终被保留
   - 捕获多尺度特征：不同层对应不同跳数的邻域信息
   - 缓解过平滑：低层嵌入的混入保持[[区分]]度
   - 隐式自连接：第0层嵌入的参与保留节点自身信息

4. **实现方式**：
   - 均匀权重：所有层赋予相等权重
   - 学习权重：通过额外参数学习最优层权重
   - [[注意力机制|注意力]]权重：根据层的重要性动态调整权重

5. **在推荐系统中的应用**：
   - [[LightGCN]]通过层组合保留原始用户-物品交[[互信息]]
   - 允许模型同时利用直接邻居和高阶邻居信息
   - 在保持协同信号传播的同时防止嵌入过度平滑

## 来源
- [[15-lightgcn.md]] — LightGCN论文中详细描述层组合策略

## 相关
- [[LightGCN]] — 层组合的典型应用实例
- [[图神经网络]] — 层组合的应用领域
- [[嵌入平滑]] — 层组合旨在解决的问题
- [[邻域聚合]] — 层组合聚合的对象