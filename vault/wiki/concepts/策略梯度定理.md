---
type: concept
status: active
confidence: 0.95
created: 2026-04-16
updated: 2026-04-16
last_accessed: 2026-04-16
source_count: 2
tags: ["强化学习", "数学", "优化"]
aliases: ["Policy Gradient Theorem", "策略梯度", "REINFORCE梯度", "∇J(θ)"]
relates_to:
  - target: "[[强化学习三大范式]]"
    type: part_of
    confidence: 0.95
  - target: "[[价值函数]]"
    type: uses
    confidence: 0.9
  - target: "[[广义优势估计]]"
    type: extends
    confidence: 0.85
  - target: "[[重要性采样]]"
    type: relates_to
    confidence: 0.8
  - target: "[[KL散度]]"
    type: relates_to
    confidence: 0.8
  - target: "[[REINFORCE算法]]"
    type: extends
    confidence: 0.95
  - target: "TRPO"
    type: extends
    confidence: 0.9
  - target: "PPO"
    type: extends
    confidence: 0.9
supersedes: null
---

# 策略梯度定理

## 概述
[[强化学习三大范式|Policy-Based]] 和 [[强化学习三大范式|Actor-Critic]] 方法的数学基石。给出参数化策略 π_θ 的目标函数 J(θ) 关于参数 θ 的梯度公式，使得可以用梯度上升直接优化策略。

## 关键内容

1. **核心公式**：
   `∇_θ J(θ) = E_{τ~π_θ} [ Σ_t ∇_θ log π_θ(a_t|s_t) · Q^π(s_t,a_t) ]`
   梯度方向由"选择动作的对数概率梯度"与"该动作的 Q 值"的乘积决定。

2. **直觉解释**：Q 值高的动作（好的选择）会增加其被选中的概率，Q 值低的动作概率降低。这是"试错"学习的精确数学表达。

3. **[[REINFORCE算法|REINFORCE]] [[算法]]**：Williams (1992) 用蒙特卡洛回报 G_t 近似 Q^π(s_t,a_t)，得到 [[REINFORCE算法|REINFORCE]] [[算法]]，是策略梯度的最简实现，但方差极高。

4. **基线（Baseline）技术**：在 Q^π(s_t,a_t) 上减去基线 b(s_t) 不改变梯度期望，但显著降低方差：
   `∇_θ J(θ) = E [ Σ_t ∇_θ log π_θ(a_t|s_t) · (Q^π(s_t,a_t) - b(s_t)) ]`
   常用 V^π(s_t) 作为基线，此时括号内变为[[价值函数|优势函数]] A^π(s_t,a_t)。

5. **TRPO/PPO 的扩展**：TRPO 在策略梯度上加 KL 散度约束保证单调改进；PPO 用 Clip 目标函数作为实用近似，避免复杂的二阶优化。

6. **[[重要性采样]]的作用**：off-policy 场景下，用行为策略 β 采集的数据估计目标策略 π 的梯度，需乘以[[重要性采样|重要性权重]] π(a|s)/β(a|s)。

## 来源
- [[raw/assets/RL-Analysis/rl_00_overview]] — 强化学习核心范式：系统性论文分析路线图
- [[raw/assets/RL-Analysis/rl_04_reinforce_trpo_ppo]] — REINFORCE/TRPO/PPO 完整推导与对比

## 相关
- [[强化学习三大范式]] — part_of
- [[价值函数]] — uses
- [[广义优势估计]] — extends
- [[重要性采样]] — relates_to
- [[KL散度]] — relates_to
- [[REINFORCE算法]] — extends
- TRPO — extends
- PPO — extends
